{
  "list_files": {
    "enabled": true,
    "confirm": false,
    "description": "list_files(path) - Lista archivos y directorios en el path especificado (por defecto '.')."
  },
  "read_file": {
    "enabled": true,
    "confirm": false,
    "description": "read_file(path) - Lee el contenido de texto de un archivo."
  },
  "write_file": {
    "enabled": true,
    "confirm": true,
    "description": "write_file(path, content) - Sobrescribe o crea un archivo con el contenido especificado."
  },
  "run_command": {
    "enabled": true,
    "confirm": true,
    "description": "run_command(command) - Ejecuta comandos shell del sistema con captura de stdout y stderr.",
    "timeout": 30
  },
  "web_search": {
    "enabled": true,
    "confirm": false,
    "description": "web_search(query) - Busca informacion en la web sobre un tema usando Wikipedia y DuckDuckGo. Escribe la consulta en el mismo idioma del usuario. Si el input es una URL, extrae directamente el contenido de esa pagina.",
    "timeout": 20
  }
}
